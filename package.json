{
  "dependencies": {
    "node-fetch": "^2.2.0",
    "pusher": "^2.1.2"
  },
  "scripts": {
    "start": "node index.js",
    "deploy": "now --docker --public --dotenv"
  },
  "devDependencies": {
    "now": "^11.3.10"
  }
}
